[build]
  functions = "netlify/functions"
  publish = "."

[functions]
  node_bundler = "esbuild"

# Redirect API calls to the serverless function
[[redirects]]
  from = "/api/waitlist"
  to = "/.netlify/functions/send-waitlist-email"
  status = 200
